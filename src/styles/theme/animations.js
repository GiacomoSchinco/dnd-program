export const animations = {
  flicker: `@keyframes flicker { 0% { opacity: 0.95 } 50% { opacity: 0.6 } 100% { opacity: 0.95 } }`,
  drift: `@keyframes drift { 0% { transform: translateY(0) } 50% { transform: translateY(-4px) } 100% { transform: translateY(0) } }`,
}
